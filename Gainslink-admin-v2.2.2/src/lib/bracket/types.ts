// Pure bracket types and re-exports from mockData.
// The actual data model lives in src/data/mockData.ts during the migration.

export type {
  MatchItem,
  MatchGame,
  MatchRound,
  TournamentStage,
  StageType,
  GroupStanding,
} from '../../data/mockData';
